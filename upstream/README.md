# upstream/

Pulled from OSINT `outputs/`, 1:1. **Nothing here is authored in Corpus.**

The pull replaces this folder wholesale, so an edit made here is overwritten without warning at the next build. `BUILT-FROM` records the OSINT commit this copy was taken from. See `DESIGN.md` §8.
