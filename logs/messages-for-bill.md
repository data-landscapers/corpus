---
type: log
title: Messages for Bill
last_reviewed: 2026-08-16
---

# Messages for Bill

*(Things an unattended run needed Bill for and could not ask. **Newest first**, one block per run. A job that runs while Bill is away never stops to put a question — it takes the deterministic path its runbook names, finishes the job, and writes what he would have been asked here. Bill reads this after a run and deletes what he has dealt with; nothing else clears it.)*

*(**It is not the run log.** `log.md` gets one line from every run, always, and is a skim of what happened. This file gets a block only when something is owed a decision or a look, so an empty file means the last runs needed nothing — which is the normal outcome.)*

*(**It is not `notes-for-osint.md`.** That file is findings that have to be actioned *in* OSINT and are carried across by hand. This one is Corpus's own business.)*

*(**Form.** A block per run, `## YYYY-MM-DD HH:MM · job`, then one bullet per item: what happened, what the run did about it, and what Bill's options are. Insert it directly under the marker below — appending puts the newest block at the bottom of a file that reads top-down, which is the failure nobody notices.)*

<!-- newest first: a new block goes directly below this line -->

## 2026-08-17 09:40 · build

- **Check H is down from 121 blocks to 64; the 57 cleared are the ones the documents could already source themselves.** You commissioned "the 80 in-document-sourced blocks". That number was mine and it was wrong: I had counted figures by splitting the checker's printed list on commas, which cut `48,847` into `48` and `847` and inflated the match rate. Recomputed from the checker's own logic the commissioned set was 62 blocks across 35 documents, and it is done. Every citation attached is a URL the same document already carried in one of its movement tables — no new source entered any report, which I verified by comparing the URL set of all 36 changed documents before and after (0 new). Three flagged blocks matched only on a substring and were left alone rather than cited to the wrong fact: COD's `3,000` (a training target, matched against uncovered localities), GIN's `4%` (matched inside `54%`), LBR's `15%` (matched inside a data-localisation clause).
- **61 blocks remain and they are the tail you have not commissioned.** These carry figures with no source anywhere in the document, so clearing them means re-opening the base or striking the figure — the expensive half, and a content decision. They sit across 30 units, worst RWA 5, MRT 4, BEN 4. **Your options**: leave them, or commission that pass separately now the cheap half is out of the way.
- **The render checks could not be re-run and I am not claiming they were.** `report-render.py --check` refuses because the catalogue is 59 records behind `raw/` — OSINT has acquired since this morning's build — and rebuilding it is stage 2 of a build, not part of this job. G's verdict cannot have moved: it tests that every link resolves in `index/`, and no document gained a link it did not already have. I, J, L and M are untouched for the same reason — no prose words added, no table, date or block changed.
- **36 documents now carry a stale `record:` digest.** A hand edit does not recompute it, and re-rendering to refresh it would need the catalogue rebuild above. Nothing checks the field; the next build will rewrite these files and report them as changed, which is accurate, because they did change.

## 2026-08-17 06:41 · build

- **Two systems in Burkina Faso may be one building, and the run folded them.** The 5 August ministerial site visit (`2026-08-06-burkina-faso-digital-transition-minister-noc-site-visit`) names a build under construction in Ouagadougou as the *Centre d'opération du Réseau Backbone national* and gives it the same supervisory function — national fibre supervision, service continuity, infrastructure security — that the January account gave the *Digital infrastructure supervision centre* already on BFA's ledger. Nothing in either record states they are the same build. I minted a separate row, then deleted it and folded the August account into the existing row with the ambiguity written into the row note and into the monthly, on the reasoning that a phantom second system inflates a report *about systems* worse than a conflation understates one. **Your options**: leave it (the note carries the uncertainty to any reader), or split it back out if you know they are two builds. Worth knowing that the two do not sit easily together on dates — the January account has a nine-month build from 2026-01-19 delivering October 2026, and the August visit puts physical execution at about 20%, though that 20% is a combined rate across this worksite and the first Maison du citoyen, so it does not settle it either way.
- **Kenya's cyber-café rules commence four weeks later than the baseline said.** The status baseline carried "From 14 August 2026 every cyber café must…", taken from an 11 August account. The regulator's own 13 August clarification cites Gazette Notice Vol. CXXVIII No. 135, published 7 August and **effective 7 September**. I corrected the baseline and the ledger to the gazette date and recorded the earlier date as superseded. Nothing needs doing; flagged because a commencement date in a published baseline is the kind of fact a reader plans around.
- *(Superseded by the 09:40 block above: the in-document-sourced half was cleared on 2026-08-17 and check H now stands at 64.)* **Check H stands at 121 blocks corpus-wide and I did not clear it.** These are narrative sentences carrying a figure with no citation on that sentence. I cleared them where I was already editing the block and left the rest: at sentence-level granularity this is a standing drafting backlog across 41 units rather than a defect of this run, and clearing it properly means re-opening sources for figures written months ago. G, I, J, L and M all pass on all 57 units, and the register is at 0. **Your options**: leave it as the visible backlog it is, or commission a pass over it as its own job — it is bounded and mechanical to *find*, just not to fix.
- **33 sources will keep appearing in the scan and cannot be considered.** Every source in this run's work order has been read except 33 whose records carry `origin_status: hold` in OSINT — `report-scan.py` drops those rather than marking them, by design, so the scan will report 18 units as holding unconsidered sources on every future run until their origins clear. Among them are several that would move ledgers if admitted, notably the HRW assessment of the US bilateral health agreements (seven units) and the Mauritanian 5G launch and data-centre items (MRT's only two sources, so MRT moved nothing this month). Nothing to do in Corpus; it clears when OSINT's origin screen clears.

## 2026-08-16 17:05 · status-init

- **A URL-splitting defect in `scripts/status_lib.py` was hiding 92 real source URLs from check A, and the 19 countries initialised before today were built against that narrower evidence set.** Cells in the AfDB dataset and the finance table that hold several URLs were split on `|`, `;` and whitespace but not on a bare comma, so `url1,url2,url3` was read as one token and every real URL in the row failed the held-set test. 108 rows across the dataset join their URLs that way. On TGO it silently dropped 10 facts at the pooling step, which is how it was found. **Fixed in this run** (commit "status_lib: split URL cells on a bare comma too"), one shared `_URL_SEP` now used by both set-builders, splitting on a comma only where a URL follows so paths containing commas survive; TGO's 10 facts came back.
  **This is the third instance of the same defect** — the pipe cost Egypt 15 facts and 46 URLs, the semicolon cost Uganda two, and each was fixed only in the one function it was found in, which is why a third separator was still there. Sharing the constant is what stops a fourth.
  **What is owed, and it is your call, not mine:** the 19 baselines already issued each dropped an unknown number of facts this way. Nothing in them is *wrong* — a dropped fact is a gap, not an error, and the asymmetry rule says gaps are the survivable kind. But they are thinner than they needed to be. The options are to leave them (cheapest, and the maintenance pass in `BUILD.md` will pick up anything material as new sources arrive), to re-pool and re-check them without re-running extraction (cheap — the fact JSONs are gone from `prep/`, so this is not actually available), or to re-run the affected countries in full (expensive). I would leave them and let maintenance close the gap, but the cost is yours to weigh.

- **Three facts were dropped before pooling on my judgement, not a rule the runbook states.** The IIAG/indicator agent attributed three specific claims to one URL out of a multi-URL row by inference, disclosed the inference, and marked them `borderline`. The borderline mechanism asks a writer to coarsen or drop the *claim* — it does not repair a *link* that may not establish the sentence, so coarsening would have left a possibly-wrong citation on the page. I dropped them. Civil registration turned out to be well covered by properly single-sourced facts, so nothing was lost. If you would rather the extraction brief said this explicitly, it is one line in `documentation/status-init-extract.md`.
