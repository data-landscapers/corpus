---
type: doc
title: Budget documents — the archetype table
last_reviewed: 2026-09-20
---

# Budget documents — the archetype table

*(How to get figures off a shape of page. Written 2026-09-20, strategic review 4 R52, from OSINT's `documentation/budget-extraction-strategies.md` — 16,505 words, published whole in the public process mirror, [data-landscapers/osint-process](https://github.com/data-landscapers/osint-process), which is where each row's full reasoning and country detail stay. One row per **structural archetype**, not per document: a new document either matches a row — note the match and add nothing — or reveals a shape none of these covers, which earns a row. A file that grows an entry per document becomes case law and stops being read.)*

**This file gets the table off the page; [`budget-extract.md`](budget-extract.md) decides what the table means.** Scope, the origin gate, the stages, the record shape, cross-footing, scale-per-table and when an absence is a finding are all there and are not repeated here.

## The toolchain, in order

1. `pdfinfo` for pages and producer — the producer is a decent archetype hint — then `pdftotext -enc UTF-8 <f> - | wc -c` over the **whole** document: a low count on the first pages is a graphical cover, not a scan.
2. `pdftotext -enc UTF-8 -layout` into `grep -n` — locate tables by their captions before extracting anything.
3. **`pdftotext -table` the moment `-layout` misaligns a wide table, before pdfplumber.** `-layout` preserves horizontal position, which is not the same as preserving columns. Its cost: digit groups return space-separated inside a number, so a numeric regex merges adjacent columns. Collapse runs of three or more spaces to a delimiter and read the columns rather than matching numbers.
4. `pdfplumber` — where both fail, and for anything needing geometry. Group `extract_words()`, or `page.chars` where glyphs go missing, by `top`; assign amounts by **right edge**, since money columns are right-aligned and a left-edge parse drifts with magnitude.
5. OCR — only where `pdftotext` returns near-zero characters across the whole document. Prose and ordinary tables come back workable; **dense wide numeric tables on a low-DPI scan do not**, returning figures wrong in the middle and plausible throughout. Where the figure matters more than the grep, render the page and read it.

**`-enc UTF-8` goes on every invocation, including the ones that only count bytes.** The default is Latin-1, and read as UTF-8 every accented term silently stops matching: one volume was recorded as containing `données` zero times when it contains it twelve.

## The archetypes

| Archetype — seen as | Shape | How to read it | Worked example |
|---|---|---|---|
| **A** — per-vote programme-budget chapter *(ZAF ENE Vote 30)* | Programme summary, narrative, then one table per programme at sub-programme × economic classification over ~7 year-columns. | Index on `^Table \d+\.`; read the scale from each table's **stub**, which varies within one document; identify the current year by header, not position. Join a line with no numeric cells to the one above. `–` is nil, parentheses are minus, growth-rate columns sit *between* the year columns, and `of which:` blocks do not sum to their parent. **In the full volume, bind by geometry**: `-layout` put one subprogramme's figures on the wrapped label below it, and a whole year was published that way. Each chapter also carries entity tables *by programme/objective/activity*, which are the entity's own spending of the vote's transfer, and bodies funded inside a vote get unnumbered chapters (`Table 6`) that a `^Table \d+\.` index misses. **A later ENE's audited columns close an earlier year**: take the year's audited outcome from the edition two years on, but only for a line whose overlapping audited years match the intervening edition's, since a restructured vote restates them. | R3,968,611 thousand appears identically in A, B, C, D and E — the archetypes check each other. |
| **B** — consolidated appropriation schedule *(ZAF Appropriation Bill)* | One block per vote: total, then programmes × five economic columns. No sub-programmes. | `grep -n -A15` the vote name. Use it for the **stage and the legal citation**, and A for detail; `Of which` sub-lines are not programmes. **The gazetted Act's text layer is glyph-shifted**: every character arrives as `(cid:n)` with n = code − 29, so `pdftotext` returns the names garbled and drops the digits. Decode `page.chars` and read. | Its transfer schedule rescued a figure the ENE's collapsed table garbled — R22,529k, illegible in A. |
| **B variant** — vote-total-only schedule *(Namibia, 3pp)* | `VOTE NO. \| TITLE \| AMOUNT`, nothing below. | **Supports no record** — every line is a ministry envelope, so the output is a stated absence, not a failed extraction. The three columns are three independent text streams and `-layout` pairs each amount with the **following** vote: bind by geometry, anchor on a known row. | Anchored on vote 09 = 12,877,000, the 29 votes cross-foot to the printed total, delta zero. |
| **C** — adjustments schedule *(ZAF AENE)* | Original + decreases + increases = adjusted, then a per-programme decomposition into named adjustment columns. | The headers give `supplementary_basis` directly: *original + adjustments = adjusted* is a **`restated-total`**, not an increment; decreases are parenthesised. It prints subprogrammes too, in R thousand. **Take the revised stage from here, not from the next ENE's adjusted column**: that column is restated to the next year's programme structure and can fold in a special appropriation enacted after the adjustments Act. | Extract at programme level: Vote 30 nets to zero at the vote while money moved between programmes. ENE 2026 prints Vote 30's 2025/26 adjusted appropriation R889.0m above the AENE's, which is Act 7 of 2026's Sentech money. |
| **D** — cross-vote statistical annexure *(ZAF Budget tables)* | One row per vote; year and stage columns under a header block two or three rows deep. | Map columns from the header block, not by count. **This archetype is right-labelled** — the vote name sits at the END of the row and the columns read leftward, so a label-then-values parser attaches every figure to the wrong vote. | Eyeball one known row against a figure held elsewhere first. |
| **E** — annual report with audited appropriation statement *(DCDT annual report)* | Narrative, then financial statements: per programme, approved → shifting → virement → **final → actual → % of final**. | `grep -n "APPROPRIATION STATEMENT"` and take the **first occurrence after the contents** — it recurs as a running header. Strip the heavy left indentation; programme names hyphenate across lines. It covers the department, not its transfers to entities. **Its approved budget is the adjusted appropriation, and its final budget adds virements made after the Adjustments Act**: those are executive, not a revised stage. | Vote 30 P5: approved R1,922.7m → final R1,768.9m → actual R755.2m, 42.7% — three stages from one row. |
| **F** — narrative audit report *(AGSA General Report)* | Prose findings with figures inside sentences, plus infographics that do not survive extraction. | Do not attempt table extraction; `grep -n -i` the terms and read the paragraphs. | **Yields no records** — context and contradiction leads. A source page, not an extraction target. |
| **G** — programme-based budget chapter and its supplementary form *(Kenya PBB)* | One chapter per vote; **Part F** is the money at programme and sub-programme × year. Supplementaries carry `Approved \| Supplementary \| Change`. | `grep -n "Total Expenditure for Vote <n>"`, read ~35 lines back. Supplementary rows wrap badly under `-layout`: map by **order**, then verify `approved + change = supplementary` per row — a failing row is a misread, not a finding. | A supplementary's `Approved` column is the *previous* supplementary's outcome, so Supp III recovers Supp II when that volume is lost. |
| **H** — accounting-office implementation review *(Kenya COB BIRR, scan + OCR)* | Per-MDA narrative stating the revision chain verbatim, then `Revised \| Expenditure \| Absorption %`. | The **narrative** is the cleanest carrier of the four stage totals; cross-foot actuals against the stated percentage, and record nothing that will not cross-foot — OCR corrupts table digits (`l2.06`). | Yields `released` and `actual` with absorption: the execution half of the series. |
| **H variant** — francophone execution annex | Native, and it arrives **embedded in an unrelated document** — per programme, initial and final dotation, engagement, paiement, taux, with capital split domestic / `Emprunt`. | `-table`, never `-layout`. Scale headers lie *between annexes of one document*. **Look inside every rectificative's explanatory note, not just the bill.** | The capital split runs the origin gate on the execution side: one programme executed 0.62% of gross credits and 1.32% of the domestic portion. |
| **I** — programme-execution canevas, native spreadsheet *(Burundi, one file per institution)* | One row per activity, nested ministère → programme → action, with a revised-budget column and execution columns after it. | `openpyxl`, `data_only=True`; scope at the **activity** text, never the programme label. **Execution columns are often unevaluated formulas returning `None`** — a canevas can yield revised appropriations and no execution rate at all. | The state labels a whole ministry's budget *Programme de digitalisation de l'administration publique*; that ministry runs the national radio. |
| **J** — procurement plan, native text *(Burundi ARMP)* | `Objet \| Mode \| classification code \| Budget prévu \| Source de financement \| dates`. | Use it where the programme volume is OCR-blocked, but **prefer the programme table where one is machine-readable** — a plan line and its programme line are the same money under two naming systems. | `Source de financement` separates state budget, own-source fund and project money. Take the highest revision. |
| **K** — the state's own budget open-data API *(Benin, 5,351 rows, no auth)* | Not a document. One row per line × year × category, and **the value columns are budget stages**; the API base sits in the portal SPA's main JS bundle. | **Corroborating, never the source of record** where a volume exists. Execution columns are usually null, and a null is a portal gap, never zero execution. Aggregates do not reconcile; economic labels permute; verify the year range per dataset, live. | One programme came back 5.3× its true value — and the API's own administrative classification sided with the workbook against its own programmatic one. That self-contradiction is the tell. |
| **L** — multi-year cross-classification estimates workbook *(Benin, annual XLSX)* | One sheet; ministry headers alternate with programme rows; **one column block per fiscal year**, nine economic columns each, including capital `intérieures / dons / prêts`. | Find the year header row by scanning the first ~10 rows for four or more integers in 2015–2040, and **derive the block stride from it** — it changes between editions. Reconcile to the printed state total. Retired codes are carried at zero. | **Prior-year columns are restated and nothing says so**: a later edition moved the whole-budget total and three programmes, one by 26%. For an appropriation, use the year's own edition. |
| **M** — LOLF full estimates volume that states its own origin split *(Burkina Faso, ~1,500pp)* | One chapter per section: four summary tables then an activity table, AE and CP over three years, capital split **Etat Seul / Contrepartie / Subvention / Prêt**. | `-table` the whole volume once; `-layout` is unusable. Grep `Section NN :` — printed page ≠ PDF page. Take programme figures from the summary tables: the **activity table double-counts personnel**, so parentheses there mean "carried at another chapitre", not negative. No regex on the money — a greedy match silently returns the **third year's** column. | Section 31: CP 20,119,980 milliers of which domestic capital 1,433,801 — and the execution annexe's `CP_INIT` is 1,433,801,000 FCFA. Run that reconciliation first on every year. |
| **M variant** — SIM_ba *budget de moyens* *(CAR)* | Section → service → TITRE → imputation, with a prior-year `Collectif` column that is the enacted revised budget. The programme and activity axis lives in the key (segments 2-4) and prints only as captions — mostly *Pas de Programme* — above the first line of each key. | **The origin gate is in the account key as well as the columns** — the imputation's last segment is `.11` domestic, `.25` grant, `.44` loan — so it survives on a single line where a column set needs the whole table. Bind labels by geometry: a label belongs to the row **below** it. A caption prints once per key, so carry it to the key's other lines across the section, and scope by the caption, not the nature: a payroll system's upkeep can be booked as IT supplies. And **a chapter opens with the *Cabinet du Ministre* block, not the section total**. | Reading that block as the section halved a year's digital investment and inverted its direction. The tell was free: its TITREs do not sum to the printed section total. |
| **M variant** — the CDMT activity table beside a SIM_ba volume *(CAR, Crystal Reports, 183pp)* | Section → programme (5 digits) → action (7) → activity (a 19-character key with `XXXX`), AE and CP over three years. **Only the domestic column is in it**: each section's CP equals the bill's `Financement intérieur`, so the origin gate is already run. | Geometry. An activity's figures sit on its code's baseline; an action's or programme's total prints about 7-8 pt **above** its own label. A long key wraps onto the next line (`…0000XXX` / `X03`). **A sibling swap still cross-foots to the parent**, so check each line on its baseline, not only the parent sums. **Where the SIM_ba volume is held, read that and use this for structure only**: its activities are the volume's directorate blocks relabelled by mission, so a 2,000,000 activity named for digital policy is a directorate's supplies and fuel, and it names no activity for lines the volume captions outright. | `-layout` gave 21,100,000 and 2,800,000 for two lines whose own baselines read 8,000,000 and 4,500,000; all 472 parents still summed. Read at activity grain, FY2026 lost the GIRAFE and PIGEON payroll lines (474,800,000) and the finance ministry's internet line and held a 718,000,000 partial in their place; re-read from the SIM_ba volume it lined up with FY2024 and FY2025. The enacted law is a 624-page scan whose annex reprints the SIM_ba tables re-edited after the bill: it matched the bill on all 42 held lines, but one service's IT-network line was gone, so check held lines on the rendered page. |
| **M variant** — *budget de moyens* as one continuous classification *(Comoros, scans)* | `CODE \| Service \| COFOG \| Designation \| prior year \| budget year`; four nature rows per service, then `Total service` and `TOTAL MINISTERE`. | **The cross-vote handle is the `Designation` column, not the amounts** — four generic natures mean no keyword reaches an IT purchase and a term search returns a clean-looking negative. Read the service *names* once, and join on the code: bodies get renamed. The embedded text layer is corrupt **and mis-paginated**, so it cannot even corroborate — render the page. | Three sweeps called cross-vote spend structurally unfindable. The country's second-largest digital line was a finance-ministry directorate named for its function. |
| **M variant** — the origin gate in the *settlement* law *(Côte d'Ivoire)* | The appropriation volume prints no source-of-financing column anywhere; the *loi de règlement*'s report carries outturn by nature × programme, investment by project, and levy delivery. | **The gate is positional, not a column**: the investment annexe opens with a co-financed block whose projects carry `Trésor / Don / Emprunt` sub-rows, and everything after it is Trésor-financed by construction. Sum the execution annexe across its four *nature* pages. | A 100% execution rate recording a collection failure: a −8,793,791,458 modification wrote the levy appropriation down by exactly what the levies failed to collect, after which spending against the remainder was total. |
| **N** — line-item estimates volume with a printed Source-of-Financing column *(Botswana, ~600pp)* | Two halves: recurrent at chart-of-accounts grain, development at project grain with `TEC \| REVISED TEC \| prior estimate \| estimate \| Balance \| SOF \| Allocation by SOF`. | `-table`; `-layout` is offset on every summary page. **`Allocation by SOF` equals the current-year estimate** — the cheapest check, and load-bearing, because **a blank cell shifts every column right of it** and a positional parse then reads the balance as the estimate. Join the halves on the department **code**: they name it differently. | **Every volume carries prior-year columns, so a later volume closes an earlier year** — `Authorised` is N−1 revised, `Actual` N−2 outturn, which is the whole strategy where a state publishes no execution report. Test the equality: a state that declines to revise reproduces the original line for line. |
| **O** — bilingual estimates workbook with a four-way source-of-finance column *(Ethiopia, XLSX)* | Header row 4, data from row 6; sheet names lie, both detail sheets carrying the national script **and** full English. `Treasury / Retained Revenue / Assistance / Loan` on every capital line. | `openpyxl`, `data_only=True`, `PYTHONIOENCODING=utf-8`. **Locate every column by header name, per year**: the layout shifts a column between editions, and a parser on last year's offsets **returns an empty set rather than raising** — it reports the country has no digital bodies. A body code ending in `0` is a function aggregate. | **The stage test is the file timestamp, not the caption** — the proclamation number stays blank even in the year published after enactment. |
| **P** — accounting-system year-end extract carrying three stages in one table *(Ethiopia IFMIS)* | `Public Body \| Approved \| Adjusted \| YTD \| Quarter`, from about row 62. Rows alternate 5-digit body codes with 7-digit economic codes — parse on code width. | `Approved` = appropriated, `Adjusted` = revised, `YTD` in the year-end file = actual. Map accounting codes to budget codes **by name and by cross-footing**; there is no crosswalk and the wrong pick produced a 2.7bn phantom variance. Coverage is partial, so **an accounting-system total is never a national total**. | **Grep the body list for `Approved = 0` with non-zero YTD first.** Donor programmes enter mid-year on the provision letting bodies book extra loan funds, so they carry zero appropriation, near-100% execution and no line in the estimates. That filter surfaced the country's two largest digital programmes. |
| **Q** — compact programme-budget summary table *(Cameroon PLF, 59 votes)* | The whole appropriation of a state in about a hundred pages, because it stops at programme grain: `CODE \| LIBELLE \| OBJECTIF \| INDICATEUR \| AE \| CP`. The vote unit is renamed between years, and the renaming carries a code restructure. | Geometry. Group words by `top`, then **re-join numeric tokens by x-gap before reading any figure**: thousands are space-separated, so `18 611 000` arrives as three words and a word-wise parse silently returns `18`. **Labels do not bind reliably** — take figures from geometry and labels from `-layout`, or a cross-vote scan returns zero hits and looks like a clean negative. | The whole state, cross-vote, in one short native file, with the objective and indicator text — where a state's digital policy targets are written down. |
| **Q variant** — the finance law in the *Journal officiel* *(Congo)* | Same compact shape, plus a ministry × `Titre` table that cross-foots against the programme table, so the capital/recurrent split is printed. | **The gazette is native where the ministry's copy is a scan**, and where it serves several renderings of one issue the **smaller file is the native one**. **Use `page.chars`, not `extract_words()`** — word extraction drops the last digit of every amount, worse than drift because it returns a wrong number rather than a right number on a wrong row. | One law: 0 characters over 238 pages on the ministry site, 615,522 in the gazette. |
| **Q variant** — the Appropriation Act's programme schedule *(Ghana, 20pp)* | 57 MDAs, 170 programmes, eight pages, four-way origin split on every line — the cross-vote instrument where estimates publish one MDA at a time. | Bind by geometry; the text stream drifts amounts several rows from their labels. **A wrapped programme label puts its own figures on the row below** — carry a pending row forward. **Programme grain is the scan's ceiling**: a digital sub-programme inside a ministry's *Management and Administration* envelope is invisible here and surfaces only from that ministry's own volume. | **The cross-vote handle is the programme name.** A digital regex over all 170 returns five rows; reading the names once finds the identity seam — `00903 Passport Administration`, GHS 148.8m, in Foreign Affairs. |
| **R** — Lusophone gazette estimates volume with a programme × financing mapa *(Cabo Verde)* | The finance law **is** the estimates volume: articulated law, `Mapa I`–`XV`, sometimes the legislature's own budget, and an earmarked-revenue annex. | **The mapa pages may be printed rotated, and rotation is not a property of the series.** A rotated page returns text character-**reversed** — native text in a rotated frame, never to be OCR'd; group by `round(x0/6)`, sort descending by `top`, reverse each token. `Mapa III` is ministry × programme **type**, so its rows are envelopes; build from **`Mapa VII`**, which prints purpose and origin together and recurs in the accounts with `OI \| ORP \| EXE`. | Three wrong answers it produced: the earmarked-revenue annex funds identity and the universal-service fund and no keyword finds it; the independent authorities sit in the **legislature's** budget; and the gender mapa is not an origin mapa. |
| **S** — programme-based budget whose narrative is native and whose money tables are pasted images *(Ghana, per MDA)* | **The trap is not that it is a scan — it is native text everywhere the money is not.** Every "is this native?" test passes. | Split the text stream on form feeds and count characters per page: the image pages come back at exactly the running footer's length. OCR with `--force` to make the narrative greppable, then **render the money page and read it** — 350 dpi, rotated where the table is landscape on a portrait page, because OCR drops whole cells out of a fifteen-column table. Nativeness varies by volume within one season — and on a **native** volume `-layout` still drifts the appropriation table, attaching the row above's figure to the line below, so render or bind by geometry there too. | **The narrative names more programmes than the appropriation funds and nothing flags it** — one country's civil-registration function was described in full and carried no line, two years running. |
| **T** — SISTAFE mapa set *(Mozambique, thirteen PDFs)* | One mapa per file. Organic classification only, **no programme axis**, the appropriating law published elsewhere or not at all; investment mapas carry `Interno \| Externo \| Total`. | Anchor every row on the organic code, cluster amount columns from their **right edges**, assign description words to the nearest code baseline. **One logical row renders on three baselines half a point apart, and a wrapped description is centred on the code's line** — first line above it, continuation below — so plain baseline grouping fails at every tolerance. Rows do not always share one baseline. Where a row's figures straddle two baselines about 6 pt apart (PESOE 2025 Mapa E), nearest-baseline binding attaches half a row to its neighbour; bind by rank instead, since every row prints a value in every column (zeros included): the k-th value down a column belongs to the k-th organic code, accepted only when each column holds exactly as many values as there are codes and every row foots. A page can be set at different right edges from the next (2025 Mapa H p. 1), so cluster columns per page. Numbers are sometimes printed without thousands separators or with a dot for the decimal comma ("653179,20", "2.568.938.12"). Foot each mapa against its own printed totals and against Mapa C before trusting it: in 2025 the law's Mapa E and Mapa H do not foot to their printed totals. | 231 rows, zero cross-foot failures, reproducing the printed grand total and all eleven column totals. **A body's provincial units can sit under a different sector prefix**, and be larger in aggregate. FY2024-2026, 110 rows: 2024 Mapa E 221 rows and all eleven Mapa F files foot to every printed column total; 2025 cell table 3,288 lines foot to 228 UGB totals, to the grand total 355,117,067.71 and to Mapa C; CGE 2024 III-5 1,656 of 1,658 rows foot (two print truncated totals). |
| **Q variant** — the répartition decrees in the *Journal officiel* *(Algeria)* | The finance law's état « B » stops at programme per ministry; one decree per ministry, gazetted within two weeks of the law, prints the programme × sub-programme table with AE and CP for each titre (4, or 7 for Finance). The tables are landscape inside portrait pages. | **Native text in a rotated frame** (text direction 0,1): read spans, a row is one x and a column one y band. Each figure is a single span, so there is no digit splitting, but `-layout` output is unusable. Bind columns from the header's *engagement* / *paiement* cells (one may be capitalised, *Paiement*), rows by rank within each column, and labels by text block. **Bold is programme, roman is sub-programme**, and a sub-programme printed bold happens (2026 Interior, *Sécurité routière*): the decree total is the check. Cross-foot every row's titres against its total, sub-programmes against their programme, programmes against the decree total, and that against article 1. Finance's article 1 excludes the non-assigned credits, which are in article 2. **The Presidency's table is annexed to the original only, and Defence has no decree**, so a body inside the Presidency's vote is not visible at all. | FY2024-2026: 36 lines, every one footing to its decree total, and every decree total equal to article 1 and to état « B ». A wrong header regex (lowercase *paiement*) misassigned the CP column on one page while its rows still footed column by column. The état « B » check caught it. |
| **U** — *morasses budgétaires*: the finance law's detail tables, one block per ministry and chapter *(Morocco, 415pp or three tomes)* | For each ministry, a summary page, then one table per chapter — personnel `1.2.1.1`, *matériel et dépenses diverses* `1.2.1.2`, investment `1.2.2.2` — nested programme (3 digits) → région (`00` services communs, `01`-`12`) → projet (2 digits; a projet with a child block carries no figure and its children do). `Code Fonc. \| Prog. \| Rég. \| Proj. \| label \| CP` and, on investment, `CE pour N+1 et suivants`. No financing column. | Geometry: every field is on the code's baseline and the label's wrapped lines sit about 5 pt above and below it, so assign a label fragment to the nearest coded line within 16 pt. `-layout` is unusable (the ministry summary tables come back with amounts on the wrong rows). Cross-foot project → `Total Région` → `Total Programme` → `TOTAL CHAPITRE`, then chapter → the law's Tableaux C and D line by line: it held for all 111 chapters in both years. Personnel is booked to one programme per ministry and is an envelope. Security and defence chapters print programme totals or the chapter only. **The own-source money is not in the volume**: the special accounts (CAS) are voted in the law's Tableau G and the autonomous services (SEGMA) in Tableaux E and F, one figure each; the settlement bill (Arabic, SEGMA tables rotated 90°) gives their outturn and in-year ceiling changes, and the report on the special accounts names what each fund paid for. CE is a forward authorisation, not the year's money. | The universal-service fund, the electronic-identity fund and the digital-modernisation fund together were 2.36bn MAD in 2024, against 1.42bn of investment credit in the digital ministry's digital-transition programme; none of them is in the morasses. |
| **N variant** — IFMIS estimates volume, programme detail to the economic item, development split by funding source *(The Gambia, ~500pp, CBMS "Budget Version")* | A one-page appropriation report by budget entity (whole dalasi), overviews in '000, then **recurrent detail** BE (2 digits) → programme (4) → sub-programme (6) → GFS item (7), and **development detail** BE → project (4) → funding source (3: `001` GLF, `410` IDA, `402` ADB…) → item with a donor tag; five year columns: N-2 actual, N-1 approved, N estimate, two projections. | Geometry: anchor each row on its left code (x < 62 pt), bind every amount to the nearest anchor by vertical centre and to a year by the header year's right edge; the code's width is its level. Every parent then sums to its children in every column but the actual one, which rounds by 1-3. **The appropriation report under `-layout` pairs each head with a neighbour's figure** — bind it too. **Read the `Budget Version` footer**: version 5 is the adopted law, version 4 the National Assembly adjustments before it, and a later posting of the "same" volume can be the earlier version. **The volume two years on closes the year**: its N-2 Actual column is the IFMIS outturn at item grain. The source block is the origin gate: `001` is domestic, every other code is a named donor. The companion PBB book prints money only per programme, recurrent and domestic development combined, and is for scope, not figures. | FY2024: the 2026 volume's 2024 Actual shows IFMIS development spending 986,500 of 54,387,350 and identity documents 152% of their appropriation; FY2025's largest digital line, WARDIP (1,945,682,500), is wholly IDA and prints no `001` block. |
| **M variant** — per-mission budget note with Amed detail tables *(Tunisia, one PDF per mission, 100-500pp, Arabic)* | Narrative and performance part, then Amed-system tables: programme × section × source of financing (`م ع م` general resources / `م ق خ م` on-lent external loans / `ح خ` special treasury account; `م ذ م ع` establishments' own resources sits outside the state budget), then detail tables for wages, operating, interventions and investment down to article-paragraph-subparagraph, and a separate table for each special treasury account. The enacted finance law prints only mission × programme, and prints those tables as page images inside an otherwise native gazette. | Reconcile each programme's `موارد الدولة` against the law's table "ت" first; where they match, the note's lines stand as appropriated. The origin gate is a column on every programme and investment line. **Arabic is set in presentation-form glyphs** — NFKC-normalise before any search, and expect some headings in a substituted font that no search reaches; read figures off the rendered page. Scale changes between tables (dinars for operating detail, thousand dinars for investment). **The cross-vote handle is the operating paragraphs 201-19-002 (software) and 201-20 (running costs of named systems), which recur in every mission**, and the grants to IT bodies such as a ministry's informatics centre. Special-account money is levy-financed (own-source) and its detail sits in its own table, not in the investment table. | Communication Technologies 2024: programme 1's 141,668,000 is 1,980,000 wages + 42,688,000 loans + 97,000,000 from the ICT fund, and the law prints only the total. |
| **A variant** — per-vote main-division estimates volume *(Namibia, Estimates of Revenue, Income and Expenditure, ~560-630pp)* | Per vote: staffing, a vote summary, then one or two pages per **main division** (the vote's administrative sub-unit) headed Vote / Main Division / Programme / Activities, an objective paragraph, and a table of economic subdivisions (010 personnel, 030 goods and services, 080 transfers, 110 capital, 200 development) over five columns: Actual N-2, Rev. Estimates N-1, Estimate N and two outer years. Under it, **Additional Notes** list named transfer recipients and, from the 2026/27 edition, the named items inside 027 Other Services. There is no programme code; the printed "Programme" label spans several main divisions, so the main division is the line. | Geometry: cluster words on baseline (5pt tolerance) and bind amounts by **right edge** to the five columns read off the years row; `-layout` drops a row's label onto the neighbour's figures. A continuation page repeats the header without the years row, and its Additional Notes carry their own years row that a naive parser takes for the table's. Check every main division: leaf items = grand total, operational + development = grand total, and the vote's main divisions = the Act's vote total (vote 09 after its statutory debt-service main division). **Each volume closes earlier years**: the next edition's Rev. Estimates column is the revised stage (test it against the Appropriation Amendment Act's amended vote totals, which fail where votes were restructured) and the edition two years on prints the unaudited actual. Additional Notes items are line grain inside a main division: hold them only where the main division is not held, and only where the group total equals the table's economic line; prior-year columns in the notes are sometimes stale copies of the last edition. | Vote 29 main division 05 ICT Development: 79,385,000 = 19,385,000 operational + 60,000,000 development, which the companion development volume splits into RAN towers (35.0m) and a regional office (25.0m). All 254, 278 and 273 main divisions foot in every column; 2026/27 Additional Notes name a deeds-registry system upgrade (15.6m) invisible at main-division grain. |
| **N variant** — development-programmes volume with inside/outside-SRF funding per project *(Namibia NPC, Development Programmes MTEF, 610pp)* | Per vote, programme and project summaries **inside** and **outside** the State Revenue Fund over the MTEF, then one page per project: funding by source (Government, inside-SRF grants/loans, outside-SRF grants/loans), composition by economic item with the funder named, description and planned activities. | Geometry, with column edges read from each page's year header; a continuation page has none, so carry the previous page's. The project code (vote/main division/serial) does **not** always match the main division the estimates book it in: cross-foot projects to the vote's development total, and to the main division only where they agree. Outside-SRF money is not appropriated and goes to `external.csv`. Read the funder from the composition table and the description both: they can disagree. | Vote 32's inside-SRF projects sum to 210,000,000, the estimates' vote development total, but its two research projects (38.0m) exceed main division 05's 34.0m. The satellite ground station prints Luxembourg Gov't as its outside source while its description says China funds it. |
| **V** — line-item estimates volume, one page per programme, origin by section and fund prefix *(Sierra Leone detailed estimates, Crystal Reports, ~600pp)* | Each page carries a DEPARTMENT / DIVISION / PROGRAMME / CLUSTER header, then sections *Personnel Emoluments*, *Other Recurrent Expenses*, *Capital Transfers*, *Development Expenditures*, each an 8-digit activity caption over account lines `FFF-AAAAAAAACC[-EEEEEE]`, then `Sub - Total`, `Programme Total`, `Expenditure Head Total`. Single year, units ("Scale amount: 1"). | **`-layout` puts labels and amounts in separate stacks, the amounts running ahead of their lines**; bind by geometry (words grouped on `top` within ~4pt; amounts right-aligned at x>700), then foot line → sub-total → programme total → head total, which holds across the whole volume. A section repeats its caption on a continuation page and must not reset its running sum. **The origin gate is the section, not a column**: *Development Expenditures* is the foreign-financed budget (its total equals the budget statement's foreign loans and grants line) and its fund prefixes (178, 182, 185 ...) do not name the donor; one foreign line in FY2026 carries prefix 101, so trust the section. The funder and loan/grant type are in the budget statement's PIP annex 5b, whose `-layout` drifts: read it by geometry. The Appropriation Act (a scan) votes only the Consolidated Fund: other charges by body, salaries and domestic capital as national sums, so enactment is tested by body totals, **and read its proviso**. | FY2025's Act withholds approval of one digital directorate's vote in a closing proviso the schedule does not flag. Activity captions are the chart of accounts' and often misname the line below; scope by the account line's own label. |
| **W** — line-item budget book by spending entity, department × object *(Liberia, 556–626pp)* | One chapter per spending entity: policy note, then *Summary by Major Object*, *Summary by Policy Area/Department*, *Summary of PSIP by Funding Source*, detailed object lines, county allocation, and Section 2 tables of department × economic class and department × object. Six year columns: N−2 actual, N−1 budget, N−1 estimated outturn, N budget, two projections. Chapters are headed GoL and carry no donor lines; external aid appears only in an image annex by partner, sector and modality. The Act, preface and annexes 3–8 are pasted images. | Native text, but stacked columns: `-layout` puts totals and values on the wrong rows. **Bind by geometry**: words share a baseline to within 2pt, and numbers go to the nearest column right edge taken from the header line (`ACTUAL / BUDGET / OUTTURN / PROJECTION`). The department axis is the programme grain; a digital line is usually a department, a PSIP project or a single named object line (`Transfer to IFMIS`, `Transfer to EMIS`). Cross-foot objects to their class and classes to the department. **A later book closes an earlier year**: the N+2 book's first column is the year's actual at object and project grain. The N+1 book's N−1 budget column is after in-year reallocation (same total, lines moved), not a revised stage. Codes and labels break between editions: the FY2025 book gives every new PSIP project the code `0238`, and the FY2026 book renumbers departments to five digits and relabels some (CSA's HRMIS department reappears as 10704 *Rural Civil Service* with identical figures), so join on figures, not labels. A *subsidy account list* after the department tables repeats department lines and is not added. | The chapter totals sum exactly to the printed grand total in FY2024 (738,859,827) and FY2026 (1,249,665,191). Every chapter is GoL money and no external digital line can be named, so the external side is `not-printed` and the domestic share is origin inferred. |
| **X** — per-vote estimates with lettered provision notes and a workbook twin *(Zimbabwe Blue Book, ~650pp PDF + XLSX)* | Each vote: narrative, then a programme summary, an economic table per programme, sub-programme lines above it, and lettered notes — `(e) Provision caters for…` — that list the projects and bodies behind one economic line. Columns: prior-year outturn, prior-year revised, in-year outturn, **approved estimates**, **statutory and other resources**, two outer years. The Act prints vote × programme only. | **Read the workbook, not the PDF** — sheet `Bluebook `, labels in column B, money in D–J, a programme tag (`V23 P2`) in K that is wrong in places; the PDF is for the page citation. Hold a sub-programme where its whole line is digital; take project grain only from a note whose list foots to its economic line or printed subtotal, and **test every note** — headings name the wrong class (a data centre under *capital grants* is the programme's machinery line), lists silently omit items, and printed subtotals miss by round sums. **The statutory column is outside the Act** — retention and levy funds such as the Universal Services Fund, passed with the Estimates but not charged by the Act; hold it as its own own-source row. | FY2026 ICT ministry: sub-programmes 466.6m + 38.6m foot to the Act's 505.2m, but the note listing its projects is 181.1m against a machinery line of 191.7m, so the projects stay inside the sub-programme. **The currency changed mid-year**: FY2024 was appropriated in Z$, and the next two volumes print 2024 in ZiG, one of them with Z$-scale values left in the column. |
| **Y** — programme-based approved estimates, all votes in one volume *(Uganda Vol. I, ~3,000pp; from FY2026/27 a ~1,300pp volume plus a ~2,200pp item Appendix)* | Per vote: V1 overview in UShs billion, **V2 by sub-subprogramme (later "vote function"), department and project in thousands**, with recurrent as Wage / NonWage and development as **GoU Dev't / External Fin.**; V5 by output (later "key service area") and item; a table naming each external project's financier (V6 or V7). The prior-year approved column is reprinted in the next volume. | Geometry, not `-layout`: group words by baseline (a row's figures sit within ~1 pt of its label), bin amounts by right edge into six columns, and cross-foot every department and project to its vote-function total and every vote to its Grand Total. **The origin gate is on the project line**: GoU Dev't is the domestic row, External Fin. goes to `external.csv`, and a GoU figure beside an external one is counterpart. **Output codes are a catch-all**: the standard "Management of ICT systems" output is used for buildings, vehicles and furniture in retooling projects, and "ICT Services" (000019) is each vote's own IT, so read the items before scoping an output. Department and project codes repeat within a vote (001 under several vote functions), so key on vote, programme, vote function and code together. Hold a whole digital department or project from V2; hold only the digital output of a mixed one from V5; take software and database items (3124xx/3134xx) where nothing above them is held. | Every FY2025/26 held line reappears unchanged in the FY2026/27 volume's prior column (127 of 127). The Appropriation Act's development schedule equals V2's GoU + external by vote; its recurrent supply runs slightly below V2 net of arrears. **Execution comes from the annual budget performance report**, Tables V3.3 (department and project) and V3.1 (output), GoU only, in UShs billion to three decimals, but on the next year's structure: lines merged across subprogrammes there cannot be matched one to one. |
| **G variant** — output-based budget with footnoted donor money, and its financial report *(Zambia Yellow Book, ~900pp, plus Financial Report Statement C)* | One chapter per head: Table 3 programme × sub-programme over three year-columns, then per programme a Table 4 economic classification that names items (transfers to bodies, projects such as *Mobile Registration/INRIS*), then outputs. Donor money appears only as a numbered footnote on the sub-programme carrying it (`- (1)`, then `World Bank Grant 1,000,000,000`). The government's Financial Report prints Statement C per head: approved, supplementary or savings declared, total authorised, expenditure, variance, by sub-programme. | Geometry for both native volumes: `-layout` puts figures several rows from their labels and runs the programme totals together (`1571,5373,83,3982,3923`). Group words by baseline and assign amounts by right edge; every programme then sums to its printed total. **Every head numbers its support programme 3499**, so key rows on head + programme. **A named economic line is often the digital line**, finer than any sub-programme: hold it with the sub-programme the narrative names, or at programme grain. **The origin gate is the footnote**: a line with no footnote is domestic by absence, so the share is inferred. The 2026 presented edition has no text layer at all (glyphs drawn as outlines); OCR at 300 dpi reproduces the earlier volumes' columns figure for figure. Join the Financial Report on the approved figure, not the code: it renumbers and relabels support sub-programmes. | The Financial Report closes a year at sub-programme grain: a K162.6m supplementary landed entirely on Smart Zambia's standards sub-programme, which spent K48.6m of K291.4m authorised. |
| **T variant** — célula orçamental table and CGE execution mapas *(Mozambique, mapas acompanhantes 2025; CGE Vol. II)* | one line per UGB x action x expense item, with Interna / Externa / Valor columns, and printed totals per UGB for funcionamento, investment and the whole. | Single baseline per line; bind by geometry, and recover action designations that print letter-spaced above and below the code line from character positions. It is the only MOZ document that names actions and investment projects outside the organic unit, so the cross-vote scan for digital lines inside mixed ministries (identity-card and passport production, e-SISTAFE communications, sector information systems) is possible only in the years it is published. Actions carry no printed subtotal; an action's figure is the sum of its expense-item lines, and the UGB's lines foot to the UGB total. Execution, CGE Volume II: Mapa III-5-1/III-5-2 (funcionamento by UGB x expense group, Dotação Final \| Realização \| %), III-6-2 (by UGB x source, âmbito REP, used for provincial units in 2025), XIII (investment by UGB and project, Interno/Externo). The UGB total pair sits about 1 pt above the code line in III-5; project codes are split over two lines (prefix+sector+year, then a 3-4 digit suffix), and the last 7 characters of the first line plus the suffix equal the célula-table project code. Dotação Final is the revised stage and Realização the actual. | FY2025: 3,288 lines foot to 228 UGB totals and to the grand total 355,117,067.71; CGE 2024 III-5: 1,656 of 1,658 rows foot (two print truncated totals). |
| Shape | How to read it | Worked example |
| One table per activity: section → programme (3 digits) → action (5) → activity (8, the first 5 digits are the action), then one row per economic paragraph with `Structure`, `Code financement` (two digits), `Code fonction`, `Coût AE`, `Coût CP`, closed by `Total activité`, `Total action`, `Total programme` and `Total section` rows. The pages are landscape tables printed rotated on portrait pages; one year is native text, the next image-only scans. | **The financing code carries no legend; the law's own capital split supplies it.** Sum each code over the whole annex and match against the ordonnance's article opening the credits (`Etat / Dons projets / Prêts projets`): codes summing exactly to the printed loans and grants are external, the rest are state-side. Native pages: `get_text('dict')` lines with direction (0,-1); a row is an x-band, columns are fixed y-ranges, and a total's label and figures can sit 6pt apart in x, so match within ±8pt. Every level cross-foots, so a parse that closes all four levels with zero mismatches is proven. Scanned pages: rotate, OCR for location only, then read held lines on the rendered page. | 2024: codes 22+42 = 206,900,000,000 (prêts projets) and 33+43 = 375,300,000,000 (dons projets) to the franc; all 2,367 activities cross-foot and the sections sum to 2,681,413,143,850. The same law's programme table has no financing column, so a reading from the summary alone records the origin as not printed. |
| **Z** — gazetted approval of one public body's budget or accounts *(Djibouti eJO, HTML, one article)* | An arrêté making a public establishment's board-adopted *budget prévisionnel* executory, or a law approving its *comptes financiers*: the visas (creating law, board deliberation, Council of Ministers date), then article 1 with products and charges as two balanced figures, or products, charges and net result. No lines, no financing split, sometimes no currency. The finance law beside it votes economic titles only and refers the ministry allocation to an unpublished annex. | Read article 1 from the publisher's HTML; the text is native. The visas are the stage test: board deliberation, then Council of Ministers, then signature, so the figure is appropriated; the signature can fall months into the year. Scope is the body's mandate from its creating law in the visas: a single-mandate digital or statistics body records whole, a multisector regulator records nothing. Accounts' charges are an accrual total and a budget's charges include investment, so hold the accounts as actual but compute no execution rate. **The Council of Ministers communiqué often carries the operating and investment split and the state subsidy**, but it is a summary: use it for context, and where the instrument itself is not gazetted, write no row. Search the eJO through its WordPress API (`/wp-json/wp/v2/search`), which lists every instrument by title. | ANSIE 2024: 451,743,675 approved, 479,221,862 charges in the accounts approved by law two years later. ANSIE 2026 (396,120,056) exists only in the Council's record, so the year carries INSTAD alone. |
| **M variant** — SIGFiP *budget de moyens* with the source of finance in the line key *(Togo, ~1,200pp)* | Section → chapitre (13-digit activity code, a body or a project) → nature → line, AE and CP in thousands. Every line prints its key `Sec. Prog/Act Chapitre Fonct Sfin Art. Par. Ligne`, and **`Sfin` is the origin gate**: 1100 Treasury, 5101 domestically financed investment, 2101 counterpart funds, 3xxx loans and 4xxx grants with the last digits naming the lender. A chapter-only print of the same system carries no key at all. | Amounts are a separate right-hand stack and `-layout` scrambles them; **bind by geometry**: cluster words within ~3.5pt of one baseline (a line's AE sits about 2pt above its CP), take AE from the column ending near x 690 and CP near x 770, and join digit groups by x-gap. A few lines print with the key on another baseline: bind them to the open chapter and let the chapter cross-foot confirm it. The programme code is the key's `Prog` segment and its name comes only from the finance law's programme table, matched by CP. A line absent from the rectificative print was cancelled, not omitted. | 2024: every one of 1,492 chapters footed on AE and CP, and the 3xxx and 4xxx lines summed exactly to the law's project loans (140,680,457) and grants (162,185,056). In the keyless 2026 chapter print the draft budget programme's external investment for the digital ministry equalled four named projects exactly, which is how their origin was inferred. |
| **N variant** — IFMIS estimates book on a cost-centre axis *(Lesotho, 319pp, "Version Name: BUDGET ESTIMATES-…")* | Budget summary, revenue by head, then **recurrent by ministry and cost centre**: head > cost centre (2 digits) > sub cost centre (2) > GFS item (6), and **capital by project**: head > cost centre > sub cost centre > project (4) > one row per funder (`GOL-Government of Lesotho`, `IDA-…`, `ADF-…`). Six columns: prior-year actual, prior-year approved, prior-year revised, budget year, two projections. A head × national-programme table (seven programmes shared by every ministry) cuts across the cost centres and is an envelope. | Geometry: the column right edges are fixed and every figure sits on its label's baseline, so bind by right edge; wrapped item labels carry no figures. Items → sub cost centre → cost centre → head foot in every column to within a maloti or two, and the heads to the Budget Summary. **The funder row is the origin gate on the capital side**; the recurrent side prints none, though the summary books a small recurrent donor total. **The capital pages head their actual column with the budget year less one while it is the budget year less two**: test it against the summary. The appropriation Act schedules heads only; the draft books print recurrent money by programme activity, not cost centre, so they serve scope, not figures. | The 2026/27 book closes 2025/26 at approved and revised and 2024/25 at actual, which is how a state whose older books sit behind a portal's buttons gets three years from one file. The digital units are sub cost centres (IFMIS, Bureau of Statistics, National Identity and Civil Registry, the ICT department, the HRMIS unit); digital activities inside other units are visible only in the drafts' activity lists. |
| **Q variant** — the finance law in the *Journal officiel* with an ADP/FDC/DONS column *(Gabon, 55-90pp)* | Articles, then a mission × programme × titre table with five columns: prior-year law, current year *hors ADP/FDC/DONS*, *ADP/FDC/DONS*, *y compris*, and the gap. Special accounts are missions 60 and up in the same table. The articles carry the rest of the own-source money as tables: *attributions de produits* by named receipt, *recettes concédées aux établissements publics* by body, allocation keys in per cent, and loan drawings and grants by project. | Native in the ministry's copy; one year's initial law may be a scan with its supplementary law native, and the supplementary law's LFI column restates the initial law, so the scan is needed only for cells the LFI column prints combined. Geometry: anchor rows on the code in the first column, bind amounts by right edge, then check *hors* + *ADP* = *y compris* and titres → programme → mission → article ceiling. **The ADP/FDC/DONS column is an origin split for earmarked receipts and grants only; loans sit in *hors* with no mark.** Match the loan table's project drawings against each programme's titre 5 before treating *hors* as domestic. The attributions table decomposes the ADP cell by receipt, and its lines sum to it. The loan table's caption may say millions while it prints units: test the total against the article's figure in words. | 2024: programme 10.416's LFI titre 5 is 6,831,210,000, exactly the E-Gabon and Gabon Digital World Bank drawings, while its ADP cell 10,837,296,911 is the ARCEP and ANINF attributions. |
| **M variant** — SIGFIP *fascicule budgétaire*, one continuous line table with the origin split in columns *(Chad, 540-740pp)* | *Budget par section par programme et chapitre titre et économique*: section → programme (3 digits) → imputation (a 16-digit key, sometimes printed 15) → titre → chapitre → article → paragraphe, with `Etat \| Don/Projets \| Prêt/Projets \| Total` for the year and, in some editions, the prior LFI first. Landscape, native, full francs. The key's last three digits track the funder (099 state, 357 a loan, 260 and 251 grants) but the columns are the gate. | Word geometry: amounts right-aligned on five fixed edges; a section, programme, titre, chapitre, article or paragraphe prints its figures on its own baseline, but an **imputation's total prints about 7 pt above its key**, so bind an unlabelled amount row to the key below it. `-layout` attaches the row above's figure to a line (2025: a 500,000,000 vehicle line onto the digital-transformation grant); don't join digit groups across a column edge. Every row foots Etat + Don + Prêt = total and every parent its children, which checks the parse across the whole volume. **Test the volume against the law's article and Tableau 2 every year**: the cover names the law whether the totals match or not. | FY2024-2026: 7,773 lines, every one footing; 2024 and 2025 equal the law and the execution reports to the million, 2026 totals 2,531.5bn against the published law's 2,530.6bn, so it was held at proposed. ANSICE, the cybersecurity agency, is one transfer line in the public-security ministry. |

## Not worth extracting

**Treasury cash-flow and monthly statements** — aggregate only, no vote or programme detail. **End-of-year statistical annexes at economic-nature grain** — machine-readable and still supporting no record, because there is no ministry and no programme in them; *native* and *useful* are different axes. **MTEF volumes** — outer years are indicative planning, not appropriations.

**Struck from this list**: the full estimates volume. An early draft called it redundant because its sector chapter duplicates the standalone extract. The duplication is beside the point — the other forty-one chapters are where a quarter or more of the spend lives.
