# site/

Rendered artefacts — HTML, PDF, and the CSVs as served. **Generated; never edited by hand.**

The next build overwrites whatever is here. To change a page, change the template in `build/`. See `DESIGN.md` §8.
