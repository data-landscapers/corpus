# build/

The pull, the renderer and the templates — **the only hand-authored code in the repo.**

`upstream/` is copied in and `site/` is generated out, so this is the only folder where editing a file is how you change something. See `DESIGN.md` §8.
