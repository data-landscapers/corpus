# The topics page

Read by `scripts/home.py` for `site/topics/`, the page the taxonomy matrix moved to on 2026-08-24 (Bill), the same week and for the same reasons as the countries matrix before it.

The page's own intro is `topics-intro` in `home.md`, not here: the same paragraph opens the Topics section on the home page, and one sentence held in two files is one sentence that will eventually say two things. What lives here is only what the matrix page carries and the home page no longer does.

## caveat

Ten Level-1 categories, rolled up from the {n} topics beneath them. A source carries as many topics as it evidences and is counted under each, so these sum to more than the total above: they measure coverage, not documents.
